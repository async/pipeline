# FrobnicateJS

The product documentation whose sentences are registered promises.

- The frobnicate command always exits zero on a clean tree.
- A second run of an unchanged tree is fully cached.
- Retries back off exponentially up to five attempts.

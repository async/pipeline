{
  "$comment": "Claim -> test map enforced by scripts/check-claims.mjs (AGENTS.md rule 2). Each anchor must appear verbatim in its source file, and every listed test title must exist in tests/**/*.test.js. Every test titled 'PROMISE: ...' must be registered here. When you add a behavioral claim to README.md, docs/, AGENTS.md, or CHANGELOG.md, register it; when you soften or remove a claim, update or remove its entry.",
  "claims": [
    {
      "id": "readme.parallel-scheduler",
      "source": "README.md",
      "anchor": "The scheduler starts ready tasks in deterministic graph order and runs independent tasks in parallel up to the configured concurrency.",
      "tests": [
        "runJob schedules ready tasks in parallel up to concurrency",
        "orders tasks deterministically with dependencies before dependents"
      ]
    },
    {
      "id": "readme.force-rerun",
      "source": "README.md",
      "anchor": "`--force` re-runs tasks while still recording fresh cache entries",
      "tests": [
        "force re-runs cached tasks and refreshes the cache entry"
      ]
    },
    {
      "id": "readme.dry-run-plan",
      "source": "README.md",
      "anchor": "`--dry-run` prints the plan with predicted cache hits without executing",
      "tests": [
        "planJob predicts cache hits without executing tasks"
      ]
    },
    {
      "id": "readme.cache-clear-gc",
      "source": "README.md",
      "anchor": "`cache clear` resets the task cache, and `gc` prunes old run records and cache entries unused for `--cache-days` days",
      "tests": [
        "cache clear and gc maintain local pipeline state"
      ]
    },
    {
      "id": "api.gc-cache-days",
      "source": "docs/api.md",
      "anchor": "`gc` prunes run records and cache entries unused for `--cache-days` days",
      "tests": [
        "cache clear and gc maintain local pipeline state",
        "cache hits refresh mtimes and gc prunes cold cache entries"
      ]
    },
    {
      "id": "local-runs.gc-cache-days",
      "source": "docs/local-runs.md",
      "anchor": "`gc` keeps the newest run records and prunes task-cache entries unused for `--cache-days` days.",
      "tests": [
        "cache clear and gc maintain local pipeline state"
      ]
    },
    {
      "id": "local-runs.cache-hit-mtime",
      "source": "docs/local-runs.md",
      "anchor": "Cache hits refresh their last-used time, and `--cache-days 0` disables cache pruning.",
      "tests": [
        "cache hits refresh mtimes and gc prunes cold cache entries"
      ]
    },
    {
      "id": "readme.run-auto-prune",
      "source": "README.md",
      "anchor": "Runs also auto-prune to the newest 50 records; set `ASYNC_PIPELINE_KEEP_RUNS` to change the limit or `0` to disable.",
      "tests": [
        "run auto-prunes records, emits json, and finds config from subdirectories"
      ]
    },
    {
      "id": "readme.log-cap",
      "source": "README.md",
      "anchor": "Task output buffers cap at 8 MiB per stream (`ASYNC_PIPELINE_MAX_LOG_BYTES`, `0` = unlimited); stored logs keep the tail.",
      "tests": [
        "task logs are capped by ASYNC_PIPELINE_MAX_LOG_BYTES with a truncation marker"
      ]
    },
    {
      "id": "how-it-works.graph-snapshot",
      "source": "docs/how-it-works.md",
      "anchor": "`graph.json` is the selected job's graph snapshot.",
      "tests": [
        "PROMISE: run records include a graph snapshot with node fingerprints"
      ]
    },
    {
      "id": "local-runs.graph-snapshot",
      "source": "docs/local-runs.md",
      "anchor": "`graph.json` records the selected job's execution order and node fingerprints.",
      "tests": [
        "PROMISE: run records include a graph snapshot with node fingerprints"
      ]
    },
    {
      "id": "how-it-works.cache-receipts",
      "source": "docs/how-it-works.md",
      "anchor": "Cache receipts under `.async/runs/<run-id>/cache/` record hit, miss, disabled, or forced-bypass decisions with the task cache key, dependency fingerprints, and graph node fingerprint, without storing file contents or secret values.",
      "tests": [
        "PROMISE: cache receipts record cache decisions without file contents"
      ]
    },
    {
      "id": "local-runs.cache-receipts",
      "source": "docs/local-runs.md",
      "anchor": "Per-task cache receipts under `.async/runs/<run-id>/cache/` explain whether each task was a cache hit, cache miss, cache-disabled run, or forced bypass, including dependency fingerprints without recording input file contents or secret values.",
      "tests": [
        "PROMISE: cache receipts record cache decisions without file contents"
      ]
    },
    {
      "id": "readme.config-walk-up",
      "source": "README.md",
      "anchor": "The CLI finds `pipeline.ts`, `pipeline.js`, `pipeline.mjs`, or `pipeline.mts` from any subdirectory by walking up.",
      "tests": [
        "discovers pipeline config in ts, js, mjs, mts order",
        "run auto-prunes records, emits json, and finds config from subdirectories"
      ]
    },
    {
      "id": "local-runs.config-discovery-order",
      "source": "docs/local-runs.md",
      "anchor": "The CLI searches the project root in this order:",
      "tests": [
        "discovers pipeline config in ts, js, mjs, mts order"
      ]
    },
    {
      "id": "readme.cache-inputs-explicit",
      "source": "README.md",
      "anchor": "Make cache behavior explicit through declared task inputs, cache refs, and task config.",
      "tests": [
        "PROMISE: per-task inputs isolate cache invalidation",
        "glob inputs are resolved deterministically and included in cache keys",
        "rejects unknown cache stores and policies"
      ]
    },
    {
      "id": "readme.metadata-without-execution",
      "source": "README.md",
      "anchor": "Read pipeline metadata without cloning sources, running `prepare`, executing tasks, or evaluating deferred shell callbacks.",
      "tests": [
        "CLI exposes metadata, matrix, source list, source sync, and namespaced run-task",
        "normalizes sources and allows declared external task refs as metadata"
      ]
    },
    {
      "id": "readme.generated-workflow",
      "source": "README.md",
      "anchor": "Generate and check a pinned GitHub Actions workflow from `pipeline.ts`.",
      "tests": [
        "github generate writes a current workflow and lock",
        "renders github workflow triggers and bootloader steps"
      ]
    },
    {
      "id": "github-actions.run-evidence-artifact",
      "source": "docs/github-actions.md",
      "anchor": "Each generated job calls `async/actions/run` to run the pipeline command, explain failures, and upload `.async/runs` as evidence.",
      "tests": [
        "renders github workflow triggers and bootloader steps"
      ]
    },
    {
      "id": "github-actions.pinned-action-manifest",
      "source": "docs/github-actions.md",
      "anchor": "All generated remote `uses:` references, including `async/actions/*`, are resolved from the central action manifest and emitted as full 40-character SHAs with a trailing human label.",
      "tests": [
        "renders github workflow triggers and bootloader steps",
        "github generate writes a current workflow and lock"
      ]
    },
    {
      "id": "github-actions.lifecycle-equivalence-fallback",
      "source": "docs/github-actions.md",
      "anchor": "Lifecycle lowering only happens for exact, whole-command publish, preview, release, or doctor lifecycle steps with representable semantics.",
      "tests": [
        "keeps lifecycle publish commands in the pipeline runner when shell semantics are not equivalent"
      ]
    },
    {
      "id": "github-actions.lifecycle-secret-scope",
      "source": "docs/github-actions.md",
      "anchor": "Generated lifecycle publish and preview steps pass secret env only to the exact Async action step that needs it.",
      "tests": [
        "renders lifecycle publish tasks as async action steps"
      ]
    },
    {
      "id": "github-actions.mutable-ref-drift-check",
      "source": "docs/github-actions.md",
      "anchor": "The drift check also fails if a committed generated workflow contains any mutable remote action ref instead of a full SHA.",
      "tests": [
        "github check rejects mutable remote action refs in generated workflows"
      ]
    },
    {
      "id": "github-actions.task-cache-step",
      "source": "docs/github-actions.md",
      "anchor": "The generated workflow persists `.async/cache` through the generated `actions/cache` step when `sync.github.cache` is true.",
      "tests": [
        "renders github workflow triggers and bootloader steps"
      ]
    },
    {
      "id": "github-actions.dependency-cache-step",
      "source": "docs/github-actions.md",
      "anchor": "`dependencyCache` controls dependency-store cache settings",
      "tests": [
        "renders github workflow triggers and bootloader steps",
        "renders github workflow with dependency cache disabled",
        "renders deno-only github workflow without package-manager install"
      ]
    },
    {
      "id": "github-actions.pnpm-runtime-setup",
      "source": "docs/github-actions.md",
      "anchor": "`setup: \"auto\"` currently resolves to the default pinned `pnpm/setup` provider. Explicit `setup: \"async\"` selects `async/actions/setup`.",
      "tests": [
        "renders github workflow triggers and bootloader steps",
        "renders explicit async setup provider",
        "renders mixed node and deno runtimes through pnpm and setup-deno providers",
        "renders github workflow with node setup provider"
      ]
    },
    {
      "id": "github-actions.pnpm-setup-single-runtime-note",
      "source": "docs/github-actions.md",
      "anchor": "The default `pnpm/setup` provider still has the historical single-runtime constraint",
      "tests": [
        "renders legacy pnpm setup with setup-node when the consumer pnpm version does not support runtime"
      ]
    },
    {
      "id": "github-actions.deno-runtime-provider-split",
      "source": "docs/github-actions.md",
      "anchor": "`async/actions/setup` accepts a newline runtime list, so repos that opt into `setup: \"async\"` can use one shared Async setup step instead of splitting runtime setup across `pnpm/setup` and `denoland/setup-deno`.",
      "tests": [
        "renders explicit async setup provider"
      ]
    },
    {
      "id": "github-actions.deno-only-setup-deno",
      "source": "docs/github-actions.md",
      "anchor": "Deno-only projects with `deno.json` or `deno.jsonc` and no `package.json` default to `deno@2`",
      "tests": [
        "renders deno-only github workflow without package-manager install"
      ]
    },
    {
      "id": "api.github-dependency-cache",
      "source": "docs/api.md",
      "anchor": "`dependencyCache: true` (the default) passes the recognized lockfile to the selected setup provider for dependency-store caching.",
      "tests": [
        "renders github workflow triggers and bootloader steps",
        "renders github workflow with dependency cache disabled",
        "renders deno-only github workflow without package-manager install"
      ]
    },
    {
      "id": "api.github-pnpm-runtime-setup",
      "source": "docs/api.md",
      "anchor": "`setup: \"auto\"` currently resolves to the default pinned `pnpm/setup` provider; explicit `setup: \"async\"` selects `async/actions/setup`.",
      "tests": [
        "renders github workflow triggers and bootloader steps",
        "renders explicit async setup provider",
        "renders github workflow with node setup provider"
      ]
    },
    {
      "id": "api.github-deno-runtime-provider-split",
      "source": "docs/api.md",
      "anchor": "explicit `runtime` accepts a string or array such as `[\"node@24\", \"deno@2\"]`.",
      "tests": [
        "renders mixed node and deno runtimes through pnpm and setup-deno providers"
      ]
    },
    {
      "id": "api.github-trigger-types",
      "source": "docs/api.md",
      "anchor": "`trigger.github` supports GitHub event `types` plus `branches`, `paths`, and `tags` filters, and `async-pipeline github generate` renders them into committed GitHub Actions YAML.",
      "tests": [
        "normalizes cron and filtered github triggers",
        "renders github workflow triggers and bootloader steps",
        "matches github jobs from event context"
      ]
    },
    {
      "id": "github-actions.release-trigger-types",
      "source": "docs/github-actions.md",
      "anchor": "`release` can match `trigger.github({ events: [\"release\"], types: [\"published\"] })`.",
      "tests": [
        "renders github workflow triggers and bootloader steps",
        "matches github jobs from event context"
      ]
    },
    {
      "id": "readme.platform-workflow",
      "source": "README.md",
      "anchor": "The checked-in workflow targets GitHub-hosted Linux (`ubuntu-latest`) and macOS (`macos-latest`) runners",
      "tests": [
        "renders github job runner labels and runner matrices"
      ]
    },
    {
      "id": "github.runner-selection",
      "source": "docs/github-actions.md",
      "anchor": "Use `job({ github: { runsOn } })` to select a hosted runner or self-hosted label set, and `job({ github: { runsOnMatrix } })` to fan a job out across multiple runner label sets",
      "tests": [
        "renders github job runner labels and runner matrices",
        "github runner config rejects invalid and conflicting settings"
      ]
    },
    {
      "id": "github.manual-selector",
      "source": "docs/github-actions.md",
      "anchor": "Manual workflow runs require selecting a single pipeline job; generated manual jobs are gated on that selection.",
      "tests": [
        "renders workflow_dispatch job selector inputs and gates manual jobs",
        "matches github jobs from event context"
      ]
    },
    {
      "id": "changelog.manual-selector",
      "source": "CHANGELOG.md",
      "anchor": "Generated GitHub workflows now expose a required manual job selector for workflow_dispatch runs, and each manual-capable job is gated by the selected job id.",
      "tests": [
        "renders workflow_dispatch job selector inputs and gates manual jobs",
        "matches github jobs from event context"
      ]
    },
    {
      "id": "github.pages-generated",
      "source": "docs/github-actions.md",
      "anchor": "GitHub Pages jobs build on pull requests and deploy from `main` or selected manual dispatch through generated build and deploy jobs.",
      "tests": [
        "renders github pages build and deploy jobs",
        "renders generated github pages jobs from sync github pages",
        "github pages config rejects invalid settings"
      ]
    },
    {
      "id": "github.sync-pages-generated",
      "source": "docs/github-actions.md",
      "anchor": "`sync.github.pages: true` infers a target from `pages`, `docs.site`, `docs`, then `build-pages`, uploads `.async/pages` as a static artifact, builds on pull requests, and deploys from `main` or selected manual dispatch.",
      "tests": [
        "renders generated github pages jobs from sync github pages",
        "generated github pages rejects missing targets and job conflicts"
      ]
    },
    {
      "id": "changelog.pages-generated",
      "source": "CHANGELOG.md",
      "anchor": "Generated GitHub workflows can now render GitHub Pages build/deploy jobs from `job({ github: { pages } })`, so Pages builds run on pull requests and deploys run from `main` or selected manual dispatch.",
      "tests": [
        "renders github pages build and deploy jobs",
        "github pages config rejects invalid settings"
      ]
    },
    {
      "id": "api.container-sandbox",
      "source": "docs/api.md",
      "anchor": "`sandbox.container(...)` declares OCI-compatible container image intent; OCI is the standard container image format used by Docker, Apple container, Podman, containerd, and registries.",
      "tests": [
        "normalizes container sandboxes and execution profiles",
        "container sandboxes resolve to selected providers without leaking undeclared env"
      ]
    },
    {
      "id": "api.execution-cli-selection",
      "source": "docs/api.md",
      "anchor": "Use `--execution <id>` to select a profile, or `--sandbox <id> --provider docker|apple-container|lima` to choose a provider for `sandbox.container(...)`.",
      "tests": [
        "runPipelineCli validates execution profiles before command-policy mock",
        "container sandboxes resolve to selected providers without leaking undeclared env"
      ]
    },
    {
      "id": "readme.container-execution-selection",
      "source": "README.md",
      "anchor": "Isolation is opt-in: select it with `--sandbox`, `--execution`, or programmatic run options; `sandbox.container(...)` is portable OCI image intent",
      "tests": [
        "runPipelineCli validates execution profiles before command-policy mock",
        "normalizes container sandboxes and execution profiles",
        "container sandboxes resolve to selected providers without leaking undeclared env"
      ]
    },
    {
      "id": "api.job-execution-profile",
      "source": "docs/api.md",
      "anchor": "`job({ execution: \"...\" })` selects a profile for local CLI defaults and generated GitHub bootloaders.",
      "tests": [
        "normalizes container sandboxes and execution profiles",
        "renders github execution profiles as runner defaults and CLI execution args"
      ]
    },
    {
      "id": "github.execution-profiles",
      "source": "docs/github-actions.md",
      "anchor": "`execution.github(...)` supplies GitHub runner defaults and generated workflows pass `--execution <id>` to the pipeline command.",
      "tests": [
        "renders github execution profiles as runner defaults and CLI execution args",
        "execution profiles reject unknown sandboxes and unsupported apple container runners"
      ]
    },
    {
      "id": "readme.sync-conflict",
      "source": "README.md",
      "anchor": "If a generated command exists but is not claimed by the lock, it fails with `ASYNC_PIPELINE_SYNC_CONFLICT`.",
      "tests": [
        "resolves package-name targets and rejects unmanaged conflicts",
        "detects stale synced task manifests and locks"
      ]
    },
    {
      "id": "sync.scripts-never-overwritten",
      "source": "docs/sync.md",
      "anchor": "generation fails with `ASYNC_PIPELINE_SYNC_CONFLICT` instead of overwriting your work",
      "tests": [
        "resolves package-name targets and rejects unmanaged conflicts"
      ]
    },
    {
      "id": "sync.drift-checkable",
      "source": "docs/sync.md",
      "anchor": "a hand-edited workflow or script fails CI with a diffable reason",
      "tests": [
        "detects stale synced task manifests and locks",
        "sync CLI generates and checks configured targets",
        "github generate writes a current workflow and lock"
      ]
    },
    {
      "id": "readme.source-prepare",
      "source": "README.md",
      "anchor": "`prepare` runs inside that source checkout before source tasks run.",
      "tests": [
        "runs a path source task with root-owned dynamic prepare and cache reuse"
      ]
    },
    {
      "id": "api.source-default-discovery",
      "source": "docs/api.md",
      "anchor": "When `pipeline` is omitted, the source checkout is searched in this order: `pipeline.ts`, `pipeline.js`, `pipeline.mjs`, `pipeline.mts`.",
      "tests": [
        "discovers omitted source pipeline in ts, js, mjs, mts order"
      ]
    },
    {
      "id": "many-repo.source-default-discovery",
      "source": "docs/many-repo-impact-runs.md",
      "anchor": "When `pipeline` is omitted, source checkouts use the same config discovery order as the root CLI: `pipeline.ts`, `pipeline.js`, `pipeline.mjs`, `pipeline.mts`.",
      "tests": [
        "discovers omitted source pipeline in ts, js, mjs, mts order"
      ]
    },
    {
      "id": "readme.namespaced-impact",
      "source": "README.md",
      "anchor": "Run many-repo impact checks with explicit dependent repos and namespaced task refs such as `storefront:test`.",
      "tests": [
        "composes source pipeline tasks into a namespaced graph"
      ]
    },
    {
      "id": "readme.task-group-flattening",
      "source": "README.md",
      "anchor": "Nested task groups flatten with `.`: `claims.default` runs as `claims`, and `claims.report` runs as `claims.report`.",
      "tests": [
        "task groups flatten default paths and resolve group-local dependencies",
        "run-task dry-run accepts local and source task group ids"
      ]
    },
    {
      "id": "readme.task-group-source-namespace",
      "source": "README.md",
      "anchor": "Source namespaces still use `:`, so `storefront:claims.report` means task `claims.report` from source `storefront`.",
      "tests": [
        "task groups keep source namespace parsing unchanged",
        "run-task dry-run accepts local and source task group ids"
      ]
    },
    {
      "id": "readme.sync-flattened-task-id",
      "source": "README.md",
      "anchor": "\"pipeline:task:claims.report\": \"async-pipeline run-task claims.report\"",
      "tests": [
        "renders opt-in raw task scripts for flattened task group ids"
      ]
    },
    {
      "id": "api.unknown-fields-rejected",
      "source": "docs/api.md",
      "anchor": "Unknown fields in the pipeline, tasks, taskDefaults, jobs, or a job's `github` config are rejected with `ASYNC_PIPELINE_UNKNOWN_FIELD`",
      "tests": [
        "rejects unknown config fields with the field name"
      ]
    },
    {
      "id": "api.declaration-metadata",
      "source": "docs/api.md",
      "anchor": "Declaration helpers attach non-enumerable metadata under `Symbol.for(\"@async/pipeline.declaration\")`, so JSON output and enumerable config shape stay unchanged.",
      "tests": [
        "declaration factories expose non-enumerable metadata without changing JSON output"
      ]
    },
    {
      "id": "api.declaration-validation",
      "source": "docs/api.md",
      "anchor": "The declaration brand is a discriminator, not trust: branded task, shell, and agent nodes are still validated, and unknown fields are still rejected.",
      "tests": [
        "portable branded task and shell nodes normalize through validation"
      ]
    },
    {
      "id": "api.section-factories",
      "source": "docs/api.md",
      "anchor": "Optional section factories such as `tasks({ ... })`, `jobs({ ... })`, and `sources({ ... })` are accepted without double wrapping; plain top-level section objects remain the default authoring style.",
      "tests": [
        "explicit section factories are accepted and mismatched sections are rejected"
      ]
    },
    {
      "id": "api.task-group-flattening",
      "source": "docs/api.md",
      "anchor": "Nested task groups flatten with `.`. A child named `default` is the group default, so `tasks.claims.default` normalizes to task id `claims`, while `tasks.claims.report` normalizes to `claims.report`. `index` remains accepted as a compatibility alias for older task groups.",
      "tests": [
        "task groups flatten default paths and resolve group-local dependencies"
      ]
    },
    {
      "id": "api.task-group-relative-deps",
      "source": "docs/api.md",
      "anchor": "Within a task group, a dependency like `dependsOn: [\"report\"]` resolves to the group-local task `claims.report` when that task exists; source refs such as `storefront:claims.report` keep using `:` for the source namespace.",
      "tests": [
        "task groups flatten default paths and resolve group-local dependencies",
        "task groups keep source namespace parsing unchanged"
      ]
    },
    {
      "id": "api.task-group-empty-task-brand",
      "source": "docs/api.md",
      "anchor": "Raw task objects still work, but branded `task({})` removes the empty-task ambiguity inside a group.",
      "tests": [
        "task groups flatten default paths and resolve group-local dependencies"
      ]
    },
    {
      "id": "api.sync-flattened-task-id",
      "source": "docs/api.md",
      "anchor": "Raw task sync is opt-in and generates names like `pipeline:task:typecheck` and `pipeline:task:claims.report`.",
      "tests": [
        "renders opt-in raw task scripts for flattened task group ids"
      ]
    },
    {
      "id": "sync.flattened-task-id",
      "source": "docs/sync.md",
      "anchor": "\"pipeline:task:claims.report\": \"async-pipeline run-task claims.report\"",
      "tests": [
        "renders opt-in raw task scripts for flattened task group ids"
      ]
    },
    {
      "id": "api.workflow-dispatch-manual-only",
      "source": "docs/api.md",
      "anchor": "On `workflow_dispatch` only jobs with a `manual` trigger run implicitly",
      "tests": [
        "matches github jobs from event context"
      ]
    },
    {
      "id": "api.run-lock",
      "source": "docs/api.md",
      "anchor": "`run` and `run-task` hold `.async/run.lock` for the duration of a run.",
      "tests": [
        "concurrent runs in the same project fail fast with ASYNC_PIPELINE_RUN_ACTIVE",
        "run locks reject active holders and reclaim stale locks"
      ]
    },
    {
      "id": "api.run-lock-stale",
      "source": "docs/api.md",
      "anchor": "A lock whose holder process is dead is reclaimed automatically",
      "tests": [
        "run locks reject active holders and reclaim stale locks"
      ]
    },
    {
      "id": "api.execution-record-schema",
      "source": "docs/api.md",
      "anchor": "Execution records (`.async/runs/<run-id>/execution.json`) and stored cache results carry `schemaVersion` (currently `1`), and records include the owning `pid` so `doctor` can tell a crashed run from a live one.",
      "tests": [
        "execution records and cache entries carry schemaVersion 1",
        "doctor warns about stale running run records"
      ]
    },
    {
      "id": "api.exit-sigint",
      "source": "docs/api.md",
      "anchor": "| `130` | Interrupted by SIGINT (Ctrl-C); tasks were terminated and the execution record finalized. |",
      "tests": [
        "PROMISE: interrupting the CLI terminates running task processes"
      ]
    },
    {
      "id": "api.exit-epipe",
      "source": "docs/api.md",
      "anchor": "| `141` | CLI output pipe closed (EPIPE, e.g. piping into `head`); tasks were terminated and the record finalized. |",
      "tests": [
        "PROMISE: a closed output pipe terminates tasks and finalizes the run"
      ]
    },
    {
      "id": "api.exit-sigterm",
      "source": "docs/api.md",
      "anchor": "| `143` | Terminated by SIGTERM; same shutdown path as SIGINT. |",
      "tests": [
        "CLI exits 143 and finalizes records after SIGTERM"
      ]
    },
    {
      "id": "api.timeout-exit",
      "source": "docs/api.md",
      "anchor": "Task-level timeouts surface as command exit code `124` inside the task result; the run itself exits `1`.",
      "tests": [
        "task timeout fails the execution and records the error"
      ]
    },
    {
      "id": "api.env-keep-runs",
      "source": "docs/api.md",
      "anchor": "| `ASYNC_PIPELINE_KEEP_RUNS` | Run-record auto-prune limit applied after each run (default `50`, `0` disables). |",
      "tests": [
        "run auto-prunes records, emits json, and finds config from subdirectories"
      ]
    },
    {
      "id": "api.env-max-log-bytes",
      "source": "docs/api.md",
      "anchor": "| `ASYNC_PIPELINE_MAX_LOG_BYTES` | Per-stream task output buffer cap in bytes (default 8 MiB, `0` = unlimited, minimum `4096`). |",
      "tests": [
        "task logs are capped by ASYNC_PIPELINE_MAX_LOG_BYTES with a truncation marker"
      ]
    },
    {
      "id": "api.env-require-environment",
      "source": "docs/api.md",
      "anchor": "| `ASYNC_PIPELINE_ENVIRONMENT` | Environment name checked by `command.requireEnvironment(...)`. |",
      "tests": [
        "requireEnvironment command policy denies unless ASYNC_PIPELINE_ENVIRONMENT matches"
      ]
    },
    {
      "id": "api.env-ci-mode",
      "source": "docs/api.md",
      "anchor": "| `CI` | When set, runs record `mode: \"ci\"`. |",
      "tests": [
        "CI env records ci execution mode"
      ]
    },
    {
      "id": "agents.cache-key-scope",
      "source": "AGENTS.md",
      "anchor": "A task's cache key depends on the content of its own declared inputs, its dependencies' cache keys, and its resolved steps",
      "tests": [
        "PROMISE: per-task inputs isolate cache invalidation",
        "dependency cache keys invalidate direct dependents",
        "cache keys omit candidate fingerprints and absolute working directories"
      ]
    },
    {
      "id": "agents.second-run-cached",
      "source": "AGENTS.md",
      "anchor": "A second run of an unchanged pipeline is fully `cached`.",
      "tests": [
        "PROMISE: a second run of an unchanged pipeline is fully cached"
      ]
    },
    {
      "id": "agents.outputs-restore-or-miss",
      "source": "AGENTS.md",
      "anchor": "A cache hit restores declared outputs, or downgrades to a recorded miss when it cannot.",
      "tests": [
        "file cache restores declared outputs on cache hit",
        "result-only file cache entries for output tasks rerun and repopulate outputs",
        "custom cache adapters store opaque blobs and restore declared outputs",
        "custom cache adapters downgrade hits to misses when output blobs are missing"
      ]
    },
    {
      "id": "agents.prune-any-depth",
      "source": "AGENTS.md",
      "anchor": "`.git/`, `.async/`, and `node_modules/` are pruned from input walks at any depth.",
      "tests": [
        "default pruning skips .git, .async, and node_modules at any depth"
      ]
    },
    {
      "id": "agents.secret-redaction",
      "source": "AGENTS.md",
      "anchor": "Resolved secret values never appear in echoed output or stored run logs.",
      "tests": [
        "resolved secret values are redacted from stored task logs",
        "PROMISE: the GitHub token never appears in publish output"
      ]
    },
    {
      "id": "agents.outputs-not-inputs",
      "source": "AGENTS.md",
      "anchor": "never on unrelated tasks' inputs, absolute machine paths, or the task's own outputs",
      "tests": [
        "PROMISE: declared outputs do not feed back into a task's own cache inputs",
        "cache inputs ignore local state directories and declared outputs"
      ]
    },
    {
      "id": "changelog.signal-no-orphans",
      "source": "CHANGELOG.md",
      "anchor": "interrupting the CLI never orphans task processes",
      "tests": [
        "PROMISE: interrupting the CLI terminates running task processes"
      ]
    },
    {
      "id": "changelog.epipe-shutdown",
      "source": "CHANGELOG.md",
      "anchor": "when CLI output is piped to a closed reader (EPIPE)",
      "tests": [
        "PROMISE: a closed output pipe terminates tasks and finalizes the run"
      ]
    },
    {
      "id": "changelog.streamed-plan",
      "source": "CHANGELOG.md",
      "anchor": "Stream CLI progress (plan and per-task status) while a run is executing",
      "tests": [
        "PROMISE: the CLI streams the plan before task output and prefixes task lines"
      ]
    },
    {
      "id": "changelog.input-cycle",
      "source": "CHANGELOG.md",
      "anchor": "Fail fast with `ASYNC_PIPELINE_INPUT_CYCLE` on `namedInputs` cycles",
      "tests": [
        "named input cycles fail fast at definePipeline time"
      ]
    },
    {
      "id": "changelog.timeout-kills-tree",
      "source": "CHANGELOG.md",
      "anchor": "Kill the whole process tree on task timeout",
      "tests": [
        "task timeout fails the execution and records the error"
      ]
    },
    {
      "id": "changelog.docker-env-allowlist",
      "source": "CHANGELOG.md",
      "anchor": "Docker sandboxes now forward only pipeline-defined env",
      "tests": [
        "docker commands forward only allowlisted env keys"
      ]
    },
    {
      "id": "changelog.container-execution-profiles",
      "source": "CHANGELOG.md",
      "anchor": "Container sandbox abstraction: `sandbox.container(...)` now declares portable OCI image intent, execution profiles select local or GitHub providers",
      "tests": [
        "normalizes container sandboxes and execution profiles",
        "renders github execution profiles as runner defaults and CLI execution args",
        "container sandboxes resolve to selected providers without leaking undeclared env"
      ]
    },
    {
      "id": "changelog.require-environment",
      "source": "CHANGELOG.md",
      "anchor": "`command.requireEnvironment(...)` is enforced",
      "tests": [
        "requireEnvironment command policy denies unless ASYNC_PIPELINE_ENVIRONMENT matches"
      ]
    },
    {
      "id": "changelog.atomic-state-writes",
      "source": "CHANGELOG.md",
      "anchor": "Write execution records, cache results, output manifests, and logs atomically",
      "tests": [
        "writeFileAtomic publishes complete files and leaves no temp files behind"
      ]
    },
    {
      "id": "changelog.run-lock",
      "source": "CHANGELOG.md",
      "anchor": "Serialize local runs with `.async/run.lock`",
      "tests": [
        "run locks reject active holders and reclaim stale locks"
      ]
    },
    {
      "id": "changelog.execution-record-version",
      "source": "CHANGELOG.md",
      "anchor": "Record `schemaVersion` and owner `pid` on execution records",
      "tests": [
        "run records include schema version and owner pid"
      ]
    },
    {
      "id": "changelog.cache-days",
      "source": "CHANGELOG.md",
      "anchor": "Refresh task-cache mtimes on cache hits and let `gc --cache-days <n>` prune cold entries.",
      "tests": [
        "cache hits refresh mtimes and gc prunes cold cache entries",
        "cache clear and gc maintain local pipeline state"
      ]
    },
    {
      "id": "changelog.explain-run-json-breaking",
      "source": "CHANGELOG.md",
      "anchor": "Change `async-pipeline explain --run <run-id> --format json` from the old context-pack array to a run-evidence object that includes execution, graph, cache receipt, log, and context-pack paths.",
      "tests": [
        "PROMISE: explain reports changed inputs and run evidence"
      ]
    },
    {
      "id": "changelog.cache-receipts",
      "source": "CHANGELOG.md",
      "anchor": "Write per-task cache receipts under `.async/runs/<run-id>/cache/` and extend `explain --run <run-id|latest>` to summarize execution, graph, cache, logs, and failure context evidence together.",
      "tests": [
        "PROMISE: cache receipts record cache decisions without file contents",
        "PROMISE: explain reports changed inputs and run evidence"
      ]
    },
    {
      "id": "changelog.github-run-evidence",
      "source": "CHANGELOG.md",
      "anchor": "Generated GitHub workflows now print `async-pipeline explain --run latest` on failure and upload `.async/runs` as pinned run-evidence artifacts.",
      "tests": [
        "renders github workflow triggers and bootloader steps"
      ]
    },
    {
      "id": "changelog.github-pnpm-runtime-fallback",
      "source": "CHANGELOG.md",
      "anchor": "Restore the setup-node/Corepack fallback for generated workflows when the consuming repo pins a pnpm version without `pnpm runtime`, so pnpm 10 projects do not fail during the setup step before pipeline tasks run.",
      "tests": [
        "renders legacy pnpm setup with setup-node when the consumer pnpm version does not support runtime"
      ]
    },
    {
      "id": "changelog.github-pnpm-setup-one-step-runtime",
      "source": "CHANGELOG.md",
      "anchor": "Use the official `pnpm/setup` runtime bootloader in generated pnpm workflows so pnpm and the Node runtime are installed in one step without depending on `actions/setup-node` or a later Corepack step.",
      "tests": [
        "renders github workflow triggers and bootloader steps",
        "renders github workflow with node setup provider"
      ]
    },
    {
      "id": "changelog.github-dependency-cache",
      "source": "CHANGELOG.md",
      "anchor": "Generated GitHub workflows now enable `actions/setup-node` dependency-store caching when `sync.github.dependencyCache` is true and a recognized package-manager lockfile exists.",
      "tests": [
        "renders github workflow triggers and bootloader steps",
        "renders github workflow with dependency cache disabled",
        "renders github workflow with node setup provider"
      ]
    },
    {
      "id": "changelog.github-pnpm-runtime-setup",
      "source": "CHANGELOG.md",
      "anchor": "Generated GitHub workflows now default `sync.github.setup: \"auto\"` to the pinned pnpm runtime shim, with `setup: \"node\"` available for the older setup-node/Corepack bootloader.",
      "tests": [
        "renders github workflow triggers and bootloader steps",
        "renders github workflow with node setup provider"
      ]
    },
    {
      "id": "changelog.package-preview-pr-only",
      "source": "CHANGELOG.md",
      "anchor": "Package preview jobs now only run for pull request events instead of trying to publish PR previews from push or release events.",
      "tests": [
        "renders generated package preview job from packagePreviews true"
      ]
    },
    {
      "id": "changelog.release-trigger-types",
      "source": "CHANGELOG.md",
      "anchor": "GitHub release triggers can now render activity `types`, and the self pipeline listens only for published releases to avoid duplicate publish runs.",
      "tests": [
        "normalizes cron and filtered github triggers",
        "renders github workflow triggers and bootloader steps",
        "matches github jobs from event context"
      ]
    },
    {
      "id": "changelog.generated-dependabot-and-previews",
      "source": "CHANGELOG.md",
      "anchor": "Add generated `sync.github.dependabotAutoMerge` and `sync.github.packagePreviews` jobs so repos can opt into guarded Dependabot auto-merge and PR package previews with `true` defaults.",
      "tests": [
        "renders generated Dependabot auto-merge workflow job",
        "renders generated package preview job from packagePreviews true",
        "generated-package-previews: verify runs and sync artifacts are current"
      ]
    },
    {
      "id": "changelog.hidden-self-hygiene",
      "source": "CHANGELOG.md",
      "anchor": "Replace direct self-hygiene scripts and task ids with `@async/hygiene`, leaving only the `hygiene` pipeline task as the release pack gate.",
      "tests": [
        "PROMISE: self hygiene gates run in the release pack chain"
      ]
    },
    {
      "id": "api.generated-dependabot-package-previews",
      "source": "docs/api.md",
      "anchor": "`dependabotAutoMerge: true` generates a guarded Dependabot approval-and-merge job for npm, Deno, and GitHub Actions dependency updates through `async/actions/dependabot-merge`. `packagePreviews: true` generates a pull-request package preview job: it finds the public root package or single public `packages/*` workspace package, runs `pack` or `build`, publishes a GitHub Packages PR preview through `async/actions/preview`, and comments install instructions on same-repo PRs. `evidence: true` adds manifest-backed evidence collection to generated jobs and a fan-in evidence job that merges downloaded manifests through `async/actions/evidence`. `sourceImpact: true` adds generated `<job>-source-plan` and `<job>-sources` jobs for source-backed jobs; the plan job writes static source metadata, `async/actions/source-impact` emits the source matrix, and the matrix job validates checkout and prepare metadata before running namespaced source tasks. `bridge` object form generates an `async-bridge` job that pulls approved Async change sets through `@async/github-app`, enforces the configured branch prefix and path allowlist, and scopes the project token to the bridge pull step. `pages: true` generates GitHub Pages build/deploy jobs from an existing docs task, defaulting to `.async/pages` and pull request, `main`, and manual triggers; `build.kind: \"prerender\"` validates static prerender output for GitHub Pages.",
      "tests": [
        "normalizes explicit sync task config and validates selected ids",
        "renders generated Dependabot auto-merge workflow job",
        "renders generated package preview job from packagePreviews true",
        "renders source impact planning and matrix jobs from sync github sourceImpact",
        "renders generated actions bridge job from sync github bridge",
        "renders generated github pages jobs from sync github pages",
        "renders prerender github pages build through async pages action"
      ]
    },
    {
      "id": "github-actions.generated-actions-bridge",
      "source": "docs/github-actions.md",
      "anchor": "`sync.github.bridge` generates an `async-bridge` job that can pull approved Async change sets, enforce the configured branch prefix and path allowlist, and apply them through `@async/github-app`.",
      "tests": [
        "normalizes explicit sync task config and validates selected ids",
        "renders generated actions bridge job from sync github bridge"
      ]
    },
    {
      "id": "changelog.generated-actions-bridge",
      "source": "CHANGELOG.md",
      "anchor": "Add `sync.github.bridge` object form so generated workflows can render an `async-bridge` job that runs on manual dispatch or a configured schedule, enforces branch-prefix and allowed-path constraints through `@async/github-app`, records bridge identity in the generated lock, and scopes the Async project token to the bridge pull step.",
      "tests": [
        "normalizes explicit sync task config and validates selected ids",
        "renders generated actions bridge job from sync github bridge"
      ]
    },
    {
      "id": "changelog.generated-evidence-fan-in",
      "source": "CHANGELOG.md",
      "anchor": "Add `sync.github.evidence` so generated workflows can collect manifest-backed evidence artifacts from each job and merge them through a generated fan-in job using `async/actions/evidence`.",
      "tests": [
        "normalizes explicit sync task config and validates selected ids",
        "renders generated package preview job from packagePreviews true"
      ]
    },
    {
      "id": "changelog.generated-source-impact",
      "source": "CHANGELOG.md",
      "anchor": "Add `sync.github.sourceImpact` so generated workflows can write static source plans, emit source matrices through `async/actions/source-impact`, validate source checkout and prepare metadata, and collect source receipts through generated evidence artifacts.",
      "tests": [
        "normalizes explicit sync task config and validates selected ids",
        "source impact plans serialize repo-local sources, matrix rows, and prepare fallback reasons",
        "renders source impact planning and matrix jobs from sync github sourceImpact"
      ]
    },
    {
      "id": "github-actions.generated-package-previews",
      "source": "docs/github-actions.md",
      "anchor": "`sync.github.packagePreviews: true` generates a `package-preview` job on pull requests. The generator finds the public root package, or the single public `packages/*` workspace package when the root package is private.",
      "tests": [
        "renders generated package preview job from packagePreviews true",
        "packagePreviews true requires explicit package when multiple public packages exist",
        "lifecycle CLI accepts custom GitHub package registry and namespace"
      ]
    },
    {
      "id": "github-actions.generated-evidence-fan-in",
      "source": "docs/github-actions.md",
      "anchor": "`sync.github.evidence: true` adds a `Collect evidence manifest` step to generated jobs and an `evidence` fan-in job that downloads `async-evidence-*` artifacts, merges their manifests through `async/actions/evidence`, and uploads a bounded index artifact.",
      "tests": [
        "renders generated package preview job from packagePreviews true"
      ]
    },
    {
      "id": "github-actions.generated-source-impact",
      "source": "docs/github-actions.md",
      "anchor": "`sync.github.sourceImpact: true` adds generated `<job>-source-plan` and `<job>-sources` jobs for source-backed pipeline jobs while leaving the original job in place.",
      "tests": [
        "source impact plans serialize repo-local sources, matrix rows, and prepare fallback reasons",
        "renders source impact planning and matrix jobs from sync github sourceImpact"
      ]
    },
    {
      "id": "github-actions.generated-dependabot-auto-merge",
      "source": "docs/github-actions.md",
      "anchor": "`sync.github.dependabotAutoMerge: true` generates a separate `dependabot-auto-merge` job on `pull_request_target`.",
      "tests": [
        "renders generated Dependabot auto-merge workflow job"
      ]
    },
    {
      "id": "changelog.doctor-stale-runs",
      "source": "CHANGELOG.md",
      "anchor": "Have `doctor` warn about unreadable run directories and stale `\"running\"` records.",
      "tests": [
        "doctor warns about stale running run records"
      ]
    },
    {
      "id": "changelog.never-running-record",
      "source": "CHANGELOG.md",
      "anchor": "Never leave an execution record in `\"running\"`",
      "tests": [
        "a throwing deferred step factory fails the task and finalizes the record",
        "PROMISE: a closed output pipe terminates tasks and finalizes the run"
      ]
    },
    {
      "id": "changelog.env-fail-fast",
      "source": "CHANGELOG.md",
      "anchor": "missing variables, or unmapped variable values fail before shell commands run",
      "tests": [
        "job env secrets fail before execution when missing",
        "mapped env vars fail before execution when unmapped"
      ]
    },
    {
      "id": "readme.github-packages-before-npm",
      "source": "README.md",
      "anchor": "Stable releases publish to GitHub Packages as `@async/pipeline` before npm, so a stable version exists on the fallback registry even when npm publishing has an issue.",
      "tests": [
        "PROMISE: stable releases mirror to GitHub Packages as @async/pipeline with the latest tag",
        "PROMISE: the release chain publishes GitHub Packages before npm, and package previews/snapshots are wired to their triggers"
      ]
    },
    {
      "id": "api.package-lifecycle-cli",
      "source": "docs/api.md",
      "anchor": "Package lifecycle networking is executed by generated GitHub Actions through `async/actions/publish` and `async/actions/preview`, not by the `@async/pipeline` npm tarball.",
      "tests": [
        "renders generated package preview job from packagePreviews true",
        "PROMISE: published package does not ship release lifecycle GitHub API fetch code"
      ]
    },
    {
      "id": "readme.release-ensure-before-publish",
      "source": "README.md",
      "anchor": "Stable release jobs create or verify the matching `v<version>` Git tag and GitHub Release through `async/actions/publish` before package publishing, and refuse to move an existing tag.",
      "tests": [
        "lifecycle CLI release ensure creates a missing tag and GitHub Release",
        "lifecycle CLI release ensure refuses to move an existing release tag",
        "PROMISE: the release chain publishes GitHub Packages before npm, and package previews/snapshots are wired to their triggers"
      ]
    },
    {
      "id": "changelog.package-lifecycle-cli",
      "source": "CHANGELOG.md",
      "anchor": "Add shared package lifecycle CLI commands for GitHub Packages previews/snapshots/releases, npm publishing, and release doctor checks with `--package <path>` package selection.",
      "tests": [
        "lifecycle CLI publishes GitHub Packages snapshots from a package path",
        "lifecycle CLI skips npm publish for an already published package and keeps public access",
        "lifecycle CLI release doctor verifies npm, GitHub Packages, GitHub Release, and release notes"
      ]
    },
    {
      "id": "changelog.release-description-parity",
      "source": "CHANGELOG.md",
      "anchor": "Treat `CHANGELOG.md` as the canonical source for semver GitHub Release descriptions: `release ensure` now updates existing GitHub Release bodies when they drift, and `release doctor` fails when any semver GitHub Release body does not match its changelog section.",
      "tests": [
        "lifecycle CLI release ensure updates stale GitHub Release notes from CHANGELOG",
        "lifecycle CLI release ensure syncs multiple semver GitHub Release notes",
        "lifecycle CLI release doctor fails when GitHub Release notes drift from CHANGELOG"
      ]
    },
    {
      "id": "changelog.release-ensure-lifecycle",
      "source": "CHANGELOG.md",
      "anchor": "Add `async-pipeline release ensure --package <path>` so generated GitHub Actions can create or verify the release tag and GitHub Release before package publishing, without local tag or release commands.",
      "tests": [
        "lifecycle CLI release ensure creates a missing tag and GitHub Release",
        "lifecycle CLI release ensure refuses to move an existing release tag",
        "PROMISE: the release chain publishes GitHub Packages before npm, and package previews/snapshots are wired to their triggers"
      ]
    },
    {
      "id": "changelog.release-doctor-registry-retry",
      "source": "CHANGELOG.md",
      "anchor": "Retry release doctor registry checks so GitHub Actions releases tolerate npm and GitHub Packages propagation after a successful publish.",
      "tests": [
        "lifecycle CLI release doctor retries registry propagation misses"
      ]
    },
    {
      "id": "changelog.npm-publish-auth-modes",
      "source": "CHANGELOG.md",
      "anchor": "npm package publishing now supports both npm trusted publishing/OIDC and token-backed publishing: token-backed runs create a temporary npmjs auth config, while tokenless runs leave auth to trusted publishing and skip the npm access command that OIDC cannot authenticate.",
      "tests": [
        "lifecycle CLI token-backed npm publish uses a temporary npmjs auth config",
        "lifecycle CLI tokenless npm publish leaves auth to trusted publishing and skips access"
      ]
    },
    {
      "id": "changelog.self-lifecycle-dogfood",
      "source": "CHANGELOG.md",
      "anchor": "Dogfood the shared lifecycle commands in the self pipeline publish chain, including release doctor verification after GitHub Packages and npm publish.",
      "tests": [
        "PROMISE: the release chain publishes GitHub Packages before npm, and package previews/snapshots are wired to their triggers"
      ]
    },
    {
      "id": "changelog.pipeline-api-surface-artifacts",
      "source": "CHANGELOG.md",
      "anchor": "Ship `@async/pipeline` API surface artifacts (`api-contract.json` and `API_SURFACE.md`) in the published package so the release ledger is available to downstream API contract checks.",
      "tests": [
        "PROMISE: package API surface artifacts are published with @async/pipeline"
      ]
    },
    {
      "id": "changelog.pipeline-api-contract-dogfood",
      "source": "CHANGELOG.md",
      "anchor": "Dogfood `@async/api-contract` in the self pipeline by syncing `pipeline:api-surface` scripts and making the release pack gate validate API surface ledgers.",
      "tests": [
        "PROMISE: API surface drift checks are wired through @async/api-contract and the release gate"
      ]
    },
    {
      "id": "changelog.self-hygiene-gates",
      "source": "CHANGELOG.md",
      "anchor": "Dogfood actionlint, publint, Are The Types Wrong, dependency-cruiser, and Knip in the self pipeline release gate so workflow YAML, publish metadata, package type resolution, dependency boundaries, and unused surface checks run before pack.",
      "tests": [
        "PROMISE: self hygiene gates run in the release pack chain"
      ]
    },
    {
      "id": "readme.main-snapshots",
      "source": "README.md",
      "anchor": "Pushes to `main` that pass the verify chain publish an immutable `0.0.0-main.sha.<sha>` snapshot to GitHub Packages and move the `main` dist-tag.",
      "tests": [
        "main snapshots publish 0.0.0-main.sha.<sha> and move the main tag while current",
        "PROMISE: a superseded main snapshot never drags the main tag backwards",
        "PROMISE: the release chain publishes GitHub Packages before npm, and package previews/snapshots are wired to their triggers"
      ]
    },
    {
      "id": "readme.pr-previews-fork-guard",
      "source": "README.md",
      "anchor": "Same-repo pull requests publish an immutable `0.0.0-pr.<n>.sha.<sha>` preview and move the `pr-<number>` dist-tag; fork pull requests never publish previews.",
      "tests": [
        "same-repo PRs publish an immutable preview version, move the pr tag, and upsert one comment",
        "PROMISE: fork pull requests never publish preview packages"
      ]
    },
    {
      "id": "readme.republish-idempotent",
      "source": "README.md",
      "anchor": "Republishing an existing version skips cleanly instead of failing, so re-dispatched publish jobs stay green.",
      "tests": [
        "PROMISE: republishing an existing version skips publish but still moves the dist-tag"
      ]
    },
    {
      "id": "api.github-permissions-unknown-fields",
      "source": "docs/api.md",
      "anchor": "Unknown permission fields fail with `ASYNC_PIPELINE_UNKNOWN_FIELD`.",
      "tests": [
        "rejects unknown github permission fields"
      ]
    },
    {
      "id": "github-actions.permissions-contents-restated",
      "source": "docs/github-actions.md",
      "anchor": "the generator restates `contents: read` automatically whenever a job grants anything else",
      "tests": [
        "renders github job packages, issues, and pull-requests permissions with a contents fallback"
      ]
    },
    {
      "id": "changelog.publish-view-discrimination",
      "source": "CHANGELOG.md",
      "anchor": "refuses to treat registry availability failures as a missing version",
      "tests": [
        "PROMISE: a registry outage during the existence check fails loudly instead of publishing blind"
      ]
    },
    {
      "id": "changelog.publish-tag-parity",
      "source": "CHANGELOG.md",
      "anchor": "enforces release tag/package.json version parity on `release` events",
      "tests": [
        "release events with a tag that does not match package.json fail before publishing",
        "release events with a matching v-prefixed tag publish normally"
      ]
    },
    {
      "id": "examples.exercised-by-release-check",
      "source": "README.md",
      "anchor": "all exercised by this repo's own `release:check`",
      "tests": [
        "basic-node-package: verify runs, re-runs cached, and sync artifacts are current",
        "many-repo-impact-run: impact job runs both source repos and matrix plans them"
      ]
    },
    {
      "id": "changelog.public-bin-entrypoint",
      "source": "CHANGELOG.md",
      "anchor": "The wrapper now calls the exported `runCliMain()` explicitly, the guard realpath-resolves `argv[1]` so symlinked bin paths count as direct execution, and a regression test drives the public bin both directly and through a symlink.",
      "tests": [
        "public package bin runs the CLI (direct and through a symlinked bin path)"
      ]
    },
    {
      "id": "changelog.run-failure-reasons-on-stderr",
      "source": "CHANGELOG.md",
      "anchor": "Failed `run`, `run-task`, and `github run` invocations now print each failed task's recorded error to stderr next to the final status line",
      "tests": [
        "run prints failed task reasons to stderr next to the final status"
      ]
    },
    {
      "id": "api.agent-cache-key",
      "source": "docs/api.md",
      "anchor": "Agent cache keys include the resolved profile id, model, and prompt — never the adapter's command path.",
      "tests": [
        "PROMISE: agent cache keys ignore the adapter binary path and change with model or prompt"
      ]
    },
    {
      "id": "api.agent-cache-default",
      "source": "docs/api.md",
      "anchor": "By default, a task that contains an `agent(...)` step normalizes to `cache: false`; it does not inherit `taskDefaults.cache`.",
      "tests": [
        "PROMISE: agent tasks do not inherit broad cache defaults"
      ]
    },
    {
      "id": "api.agent-cache-replay",
      "source": "docs/api.md",
      "anchor": "A cached agent task replays its declared outputs without invoking the adapter.",
      "tests": [
        "PROMISE: a cached agent task replays declared outputs without re-invoking the adapter"
      ]
    },
    {
      "id": "api.agent-transcript-redaction",
      "source": "docs/api.md",
      "anchor": "A request/response transcript is written to `.async/runs/<run-id>/agents/<task>.jsonl`. Transcripts and task logs redact resolved secret values.",
      "tests": [
        "PROMISE: an agent step runs the selected profile and records a redacted transcript under the run's agents directory"
      ]
    },
    {
      "id": "api.agent-env-selection",
      "source": "docs/api.md",
      "anchor": "both `use` and `model` accept `env.var(...)`, resolved at run time from the task env",
      "tests": [
        "PROMISE: env.var(...) selects the agent profile and model at run time"
      ]
    },
    {
      "id": "changelog.agent-cache-replay",
      "source": "CHANGELOG.md",
      "anchor": "keys include the resolved profile id, model, and prompt — never the adapter's command path — so a cached agent task replays declared outputs without invoking the adapter",
      "tests": [
        "PROMISE: agent cache keys ignore the adapter binary path and change with model or prompt",
        "PROMISE: a cached agent task replays declared outputs without re-invoking the adapter"
      ]
    },
    {
      "id": "api.context-pack-on-failure",
      "source": "docs/api.md",
      "anchor": "On task failure the runner writes a context pack to `.async/runs/<run-id>/context/<task>.json`: the error, a redacted log tail, the reproduction command, and the input diff against the task's last passing cache entry — content digests only, never file contents.",
      "tests": [
        "PROMISE: failed tasks write a context pack with a redacted log tail and an input diff against the last passing run"
      ]
    },
    {
      "id": "api.context-pack-baseline-missing",
      "source": "docs/api.md",
      "anchor": "A task that has never passed reports `baselineMissing` instead of a diff.",
      "tests": [
        "PROMISE: a task that has never passed reports a missing baseline instead of a diff"
      ]
    },
    {
      "id": "api.cache-input-manifests",
      "source": "docs/api.md",
      "anchor": "every cache entry persists a per-file digest manifest (`inputs.json`) for the input state that produced it",
      "tests": [
        "PROMISE: cache entries persist per-file input digests for the entry's input state"
      ]
    },
    {
      "id": "api.cache-receipts",
      "source": "docs/api.md",
      "anchor": "Every run also writes cache receipts under `.async/runs/<run-id>/cache/<task>.json`.",
      "tests": [
        "PROMISE: cache receipts record cache decisions without file contents"
      ]
    },
    {
      "id": "api.custom-cache-adapter-required-methods",
      "source": "docs/api.md",
      "anchor": "Only `get` and `put` are required.",
      "tests": [
        "custom cache stores can carry executable blob adapters",
        "custom cache adapters store opaque blobs and restore declared outputs"
      ]
    },
    {
      "id": "api.cache-store-lifecycle-hooks",
      "source": "docs/api.md",
      "anchor": "The built-in `file`, `memory`, and `redisCache(...)` adapters implement the optional lifecycle methods",
      "tests": [
        "file cache store adapter lists deletes touches and prunes blob keys",
        "memory cache store adapter lists deletes touches and prunes blob keys",
        "redis cache store adapter lists deletes touches and prunes blob keys"
      ]
    },
    {
      "id": "api.custom-cache-adapter-boundary",
      "source": "docs/api.md",
      "anchor": "Adapters treat keys as opaque strings and must not inspect source inputs, calculate cache keys, or decide whether an entry is valid.",
      "tests": [
        "custom cache adapters store opaque blobs and restore declared outputs",
        "custom cache adapters downgrade hits to misses when output blobs are missing",
        "custom cache adapter read failures fail loudly instead of becoming misses"
      ]
    },
    {
      "id": "api.redis-cache-executes-without-client-dependency",
      "source": "docs/api.md",
      "anchor": "the runner uses Node sockets and does not depend on a Redis npm package",
      "tests": [
        "redisCache executes through a supplied Redis URL without a client dependency"
      ]
    },
    {
      "id": "api.explain-diff-inputs",
      "source": "docs/api.md",
      "anchor": "`explain <task> --diff-inputs` reports the files that changed since the task last passed",
      "tests": [
        "PROMISE: explain reports changed inputs and run evidence"
      ]
    },
    {
      "id": "api.explain-run-evidence",
      "source": "docs/api.md",
      "anchor": "async-pipeline explain --run <run-id|latest>      # human summary of run evidence",
      "tests": [
        "PROMISE: explain reports changed inputs and run evidence"
      ]
    },
    {
      "id": "api.mcp-read-only-default",
      "source": "docs/api.md",
      "anchor": "The MCP server is read-only by default: `run_job` is exposed only when the server is started with `--allow-run`.",
      "tests": [
        "PROMISE: the MCP server answers initialize and exposes read-only inspection tools without run_job by default"
      ]
    },
    {
      "id": "api.mcp-run-job-evidence",
      "source": "docs/api.md",
      "anchor": "`run_job` acquires the same run lock, writes the same records, and replays the same cache as a CLI run",
      "tests": [
        "PROMISE: MCP run_job with --allow-run runs the same scheduler and records run evidence"
      ]
    },
    {
      "id": "api.agent-stdout-to",
      "source": "docs/api.md",
      "anchor": "`stdoutTo` lands the adapter's stdout as a task artifact after a successful step",
      "tests": [
        "PROMISE: agent stdoutTo lands the adapter's stdout as a task artifact and caches it like any output"
      ]
    },
    {
      "id": "api.agent-outputs-doctor-warning",
      "source": "docs/api.md",
      "anchor": "`doctor` warns when an agent task declares no outputs",
      "tests": [
        "PROMISE: doctor warns when an agent task declares no outputs"
      ]
    },
    {
      "id": "readme.deno-only-task-sync",
      "source": "README.md",
      "anchor": "Deno-only roots with `deno.json` or `deno.jsonc` and no `package.json` default generated task commands to `deno run -A npm:@async/pipeline/cli`; set `sync.command` when you want a local wrapper such as `deno task async-pipeline`.",
      "tests": [
        "sync.tasks true renders Deno CLI commands for a Deno-only root",
        "deno-only-pipeline: verify runs through Deno and sync artifacts are current"
      ]
    },
    {
      "id": "readme.github-runtime-defaults",
      "source": "README.md",
      "anchor": "Node projects default generated GitHub workflows to `node@24`; Deno-only projects with `deno.json` or `deno.jsonc` and no `package.json` default to `deno@2`; mixed projects can set `sync.github.runtime` to both.",
      "tests": [
        "renders github workflow triggers and bootloader steps",
        "renders deno-only github workflow without package-manager install",
        "renders mixed node and deno runtimes through pnpm and setup-deno providers"
      ]
    },
    {
      "id": "readme.deno-cli-subpath",
      "source": "README.md",
      "anchor": "Deno-only repos can omit `package.json` and run the published CLI through Deno's npm compatibility layer:",
      "tests": [
        "PROMISE: package exports the CLI subpath for Deno npm entrypoints",
        "deno-only-pipeline: verify runs through Deno and sync artifacts are current"
      ]
    },
    {
      "id": "api.sync-command-runtime-defaults",
      "source": "docs/api.md",
      "anchor": "`sync.command` defaults to `async-pipeline` and is used by generated task commands and generated GitHub workflow commands.",
      "tests": [
        "PROMISE: package exports the CLI subpath for Deno npm entrypoints",
        "normalizes sync command and github runtime declarations",
        "sync.tasks true renders Deno CLI commands for a Deno-only root",
        "renders deno-only github workflow without package-manager install"
      ]
    },
    {
      "id": "api.deno-only-generated-commands",
      "source": "docs/api.md",
      "anchor": "Deno-only roots with `deno.json` or `deno.jsonc` and no `package.json` render generated task and workflow commands through `deno run -A npm:@async/pipeline/cli` unless `sync.command` is set.",
      "tests": [
        "sync.tasks true renders Deno CLI commands for a Deno-only root",
        "renders deno-only github workflow without package-manager install",
        "deno-only-pipeline: verify runs through Deno and sync artifacts are current"
      ]
    },
    {
      "id": "api.requires-runtime-enforcement",
      "source": "docs/api.md",
      "anchor": "`requires.runtime` enforces declared `node` and `deno` runtimes before task commands execute; `shell` remains a generic runtime declaration.",
      "tests": [
        "requires.runtime fails before running a missing runtime command",
        "doctor checks declared runtimes without assuming node and pnpm"
      ]
    },
    {
      "id": "github.runtime-defaults",
      "source": "docs/github-actions.md",
      "anchor": "Package projects default to `node@<nodeVersion>`; Deno-only projects with `deno.json` or `deno.jsonc` and no `package.json` default to `deno@2`; explicit `runtime` accepts a string or array such as `[\"node@24\", \"deno@2\"]`.",
      "tests": [
        "renders github workflow triggers and bootloader steps",
        "renders deno-only github workflow without package-manager install",
        "renders mixed node and deno runtimes through pnpm and setup-deno providers"
      ]
    },
    {
      "id": "github.deno-only-install",
      "source": "docs/github-actions.md",
      "anchor": "Deno-only generated workflows run `deno install` instead of package-manager install.",
      "tests": [
        "renders deno-only github workflow without package-manager install"
      ]
    },
    {
      "id": "sync.deno-only-generated-tasks",
      "source": "docs/sync.md",
      "anchor": "Deno-only repos can omit `package.json`: with `deno.json` or `deno.jsonc` at the root, generated tasks default to `deno run -A npm:@async/pipeline/cli`.",
      "tests": [
        "sync.tasks true renders Deno CLI commands for a Deno-only root",
        "deno-only-pipeline: verify runs through Deno and sync artifacts are current"
      ]
    },
    {
      "id": "examples.deno-only-pipeline",
      "source": "examples/README.md",
      "anchor": "A no-`package.json` project using Deno tasks, Deno runtime requirements, and Deno-native generated GitHub workflow commands.",
      "tests": [
        "deno-only-pipeline: verify runs through Deno and sync artifacts are current"
      ]
    },
    {
      "id": "changelog.deno-only-pipeline-support",
      "source": "CHANGELOG.md",
      "anchor": "Add first-class Deno-only pipeline support: export the CLI as `@async/pipeline/cli`, add `sync.command` and `sync.github.runtime`, render Deno-only task and GitHub workflow commands without a `package.json`, enforce declared Node and Deno task runtimes before command execution, and ship a no-`package.json` Deno-only example.",
      "tests": [
        "normalizes sync command and github runtime declarations",
        "sync.tasks true renders Deno CLI commands for a Deno-only root",
        "renders deno-only github workflow without package-manager install",
        "renders mixed node and deno runtimes through pnpm and setup-deno providers",
        "requires.runtime fails before running a missing runtime command",
        "deno-only-pipeline: verify runs through Deno and sync artifacts are current"
      ]
    },
    {
      "id": "changelog.github-sync-pages",
      "source": "CHANGELOG.md",
      "anchor": "Add `sync.github.pages` so generated GitHub Pages build/deploy jobs can be declared at the GitHub sync level from an existing docs task, letting repos remove repetitive local Pages job declarations while keeping `job({ github: { pages } })` as the lower-level compatibility form.",
      "tests": [
        "normalizes explicit sync task config and validates selected ids",
        "renders generated github pages jobs from sync github pages",
        "generated github pages rejects missing targets and job conflicts",
        "renders github pages build and deploy jobs"
      ]
    },
    {
      "id": "changelog.github-deno-runtime-provider-split",
      "source": "CHANGELOG.md",
      "anchor": "Render generated Deno runtime setup through pinned `denoland/setup-deno` steps instead of `pnpm runtime set deno ...`, while keeping `pnpm/setup` as the package-project Node and pnpm bootloader.",
      "tests": [
        "renders deno-only github workflow without package-manager install",
        "renders mixed node and deno runtimes through pnpm and setup-deno providers"
      ]
    },
    {
      "id": "changelog.agent-claims-repair-example",
      "source": "CHANGELOG.md",
      "anchor": "mock adapter profile emitting an applicable unified diff with one line of context per hunk, full circle exercised by the examples suite",
      "tests": [
        "agent-claims-repair: verify is green, the stale fixture fails, and the mock agent proposes an applicable patch"
      ]
    },
    {
      "id": "changelog.config-discovery-order",
      "source": "CHANGELOG.md",
      "anchor": "Change default pipeline config discovery order to `pipeline.ts`, `pipeline.js`, `pipeline.mjs`, `pipeline.mts`.",
      "tests": [
        "discovers pipeline config in ts, js, mjs, mts order"
      ]
    },
    {
      "id": "changelog.source-default-discovery",
      "source": "CHANGELOG.md",
      "anchor": "Source declarations that omit `pipeline` now use the same default discovery order as the root CLI.",
      "tests": [
        "discovers omitted source pipeline in ts, js, mjs, mts order"
      ]
    }
  ]
}
